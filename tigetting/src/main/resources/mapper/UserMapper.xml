<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.tigetting.mapper.UserMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, email, password_hash, phone_number, role, created_at, updated_at)
        VALUES (#{username}, #{email}, #{passwordHash}, #{phoneNumber}, #{role}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findById" resultType="User">
        SELECT * FROM users WHERE user_id = #{id}
    </select>

    <select id="findByUsername" resultType="User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findByEmail" resultType="User">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>

</mapper>
